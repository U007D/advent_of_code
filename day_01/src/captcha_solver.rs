#[derive(Clone, Default, Debug)]
pub struct CaptchaSolver {}

impl CaptchaSolver {
    pub fn new() -> Self {
        Self {}
    }

    pub fn sum_when_matches_next(&self, digits: &str) -> usize {
        0
    }
}
